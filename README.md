This code get music\\image file data and you can change it.

* Copy the path of file and enter it in input.
* If you got any error that related to the path, instead of using \\ use \\\\.
* For install requirements : Open terminal in this folder and write this => pip install -r requirements.txt
